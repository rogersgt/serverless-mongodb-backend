{
  "name": "serverless-mongodb-backend",
  "version": "1.0.0",
  "description": "Serverless MongoDB Quick Start API",
  "main": "dist/index.js",
  "scripts": {
    "build": "yarn eslint src && babel src --out-dir dist",
    "test:delete": "yarn build && SLS_DEBUG=* serverless invoke local -f mongodbCrud --stage dev -p test/events/delete.json",
    "test:get": "yarn build && SLS_DEBUG=* serverless invoke local -f mongodbCrud --stage dev -p test/events/get.json",
    "test:post": "yarn build && SLS_DEBUG=* serverless invoke local -f mongodbCrud --stage dev -p test/events/post.json",
    "test:put": "yarn build && SLS_DEBUG=* serverless invoke local -f mongodbCrud --stage dev -p test/events/put.json"
  },
  "author": "Grady Rogers",
  "license": "ISC",
  "dependencies": {
    "babel-polyfill": "6.26.0",
    "mongoose": "5.4.x",
    "uuid": "3.3.2"
  },
  "devDependencies": {
    "babel-cli": "6.26.0",
    "babel-core": "6.26.3",
    "babel-plugin-transform-object-rest-spread": "6.26.0",
    "babel-preset-env": "1.7.0",
    "colors": "^1.3.3",
    "dotenv": "6.2.0",
    "eslint": "5.12.0",
    "generate-password": "1.4.1",
    "mongodb": "3.1.13"
  }
}
