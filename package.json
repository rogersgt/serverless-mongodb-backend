{
  "name": "serverless-mongodb-backend",
  "version": "1.0.0",
  "description": "Serverless MongoDB Quick Start API",
  "main": "dist/index.js",
  "engines": {
    "node": "^8.x"
  },
  "scripts": {
    "build": "yarn eslint src && babel src --out-dir dist",
    "db:local": "dotenv docker-compose up -d mongo",
    "deploy": "yarn build && dotenv node deploy",
    "test:delete": "yarn build && dotenv serverless invoke local -f mongodbCrud --stage dev -p test/events/delete.json",
    "test:get": "yarn build && dotenv serverless invoke local -f mongodbCrud --stage dev -p test/events/get.json",
    "test:post": "yarn build && dotenv serverless invoke local -f mongodbCrud --stage dev -p test/events/post.json",
    "test:put": "yarn build && dotenv serverless invoke local -f mongodbCrud --stage dev -p test/events/put.json"
  },
  "author": "Grady Rogers",
  "license": "MIT",
  "dependencies": {
    "babel-polyfill": "6.26.x",
    "child-process-promise": "^2.2.1",
    "mongoose": "5.4.x",
    "uuid": "3.3.x"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "colors": "1.3.x",
    "dotenv": "6.2.x",
    "dotenv-cli": "2.0.x",
    "eslint": "^6.0.1",
    "generate-password": "1.4.x",
    "mongodb": "^3.2.7",
    "rhinocloud-sdk": "1.1.x"
  }
}
